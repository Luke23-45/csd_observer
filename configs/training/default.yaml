# §9 training group (default). Values pinned to the store.py node (see
# tests/config/test_yaml_alignment.py).
enabled: true
epochs: 50
batch_size: 64
lr: 0.001
weight_decay: 0.00001
patience: 5
spectral_radius_weight: 0.01
spectral_threshold: 0.95
scheduler_eta_min: 0.000001
label_window: 60
num_workers: 0
deterministic: true
accelerator: auto
devices: auto
progress_bar: false