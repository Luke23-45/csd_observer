# §5.2 synthetic Hopf dataset (generator `classic`, radial alarm mode).
# Values pinned to the store.py node (see tests/config/test_yaml_alignment.py).
name: synthetic_hopf
source: synthetic
bif_type: hopf
feature_mode: radial
licence: project-generated
generator: classic
difficulty: standard
n_trajectories: 500
max_length: 200
seed: 42
split:
  seed: 42
  train_frac: 0.6
  val_frac: 0.2
  replicate_based: true