# §7 evaluation group: classic protocol (k=1, single-window alarm).
# Values pinned to the store.py node (see tests/config/test_yaml_alignment.py).
name: baseline_classic
k_persist: 1
fpr_target: 0.05
early_start_delta: 50.0
early_end_delta: 5.0