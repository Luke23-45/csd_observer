# §6 model group: full block selection (every method present; per-method
# yamls select a single block). `in_channels`/`stride` are None on the
# nodes and therefore omitted (auto-inferred at runtime). Values pinned
# to the store.py node (see tests/config/test_yaml_alignment.py).
var_csd:
  window_size: 30
ac1_csd:
  window_size: 30
skew_csd:
  window_size: 30
sratio_csd:
  window_size: 30
retrate_csd:
  window_size: 30
dfa_csd:
  window_size: 100
dmd_csd:
  window_size: 30
  embedding_dim: 6
  rank: 2
lstm:
  hidden_size: 64
  num_layers: 1
  dropout: 0.1
  score_batch_size: 64
tcn:
  hidden_size: 32
  kernel_size: 5
  levels: 3
  score_batch_size: 64
patchtst:
  patch_len: 16
  d_model: 64
  n_heads: 2
  n_layers: 2
  mlp_ratio: 4.0
  dropout: 0.1
  score_batch_size: 64